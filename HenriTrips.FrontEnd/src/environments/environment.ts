export const environment = {
  production: false,
   //apiUrl: 'https://localhost:7173/api'  // local development URL with /api at the end
  apiUrl: 'http://localhost:5000/api'  // using docker URL with /api at the end
};
